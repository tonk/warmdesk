meta {
  name: Timer Targets
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/timer/targets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Projects and customers the timer can be started on.
}
