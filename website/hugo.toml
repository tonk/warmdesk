baseURL = "https://tonk.github.io/warmdesk/"
languageCode = "en-us"
title = "WarmDesk"
theme = "warmdesk"
enableRobotsTXT = true
enableEmoji = true

[params]
  description = "Self-hosted project management for modern teams"
  author = "Ton Kersten"
  github = "https://github.com/tonk/warmdesk"
  email = "tonk@smartowl.nl"
  tagline = "Own Your Workflow"
  subtitle = "A single Go binary covering Kanban boards, Scrum sprints, team chat with one-to-one voice & video calls, helpdesk ticketing with SLA, customer management, time tracking, and Git integration — owned and operated by you."
  # Social preview image (absolute URL built via absURL). Prefer ≥1200×630 PNG/JPEG for best crawler support.
  og_image = "images/logo-full.svg"
  # Release info — update both this and warmdesk-version in [markup.asciidocext.attributes] on each release
  warmdesk_version = "v0.30.0"
  release_date     = "2026-09-24"
  # Blog post slug for the current release (linked from the homepage "What's new" strip)
  release_blog     = "blog/ticket-api-card-updates/"
  # Release highlights shown in the "What's new" strip on the homepage (2-3 bullets)
  release_highlights = [
    "**Added — Update cards through the Ticket API** — change a card's title, description, priority, dates, or story points, or close and reopen it, with a partial `PATCH` that only touches what you send.",
    "**Added — Cards by key, lanes by name** — use `ANSI-12` instead of a numeric id, and `\"column\": \"Done\"` instead of a `column_id`; creating a card now also takes priority, dates, and story points.",
  ]

[markup]
  [markup.asciidocext]
    backend = "html5"
    extensions = []
    failureLevel = "fatal"
    noHeaderOrFooter = true
    preserveTOC = false
    safeMode = "unsafe"
    sectionNumbers = false
    trace = false
    verbose = false
    workingFolderCurrent = false
    [markup.asciidocext.attributes]
      "source-highlighter" = "highlight.js"
      "sectanchors" = ""
      "sectlinks" = ""
      "warmdesk-version" = "v0.30.0"

  # Chroma off — fenced Markdown code stays plain <pre><code>; AsciiDoc uses hljs (see theme head).
  [markup.highlight]
    codeFences = false

[security]
  [security.exec]
    allow = ['^(dart-)?sass(-embedded)?$', '^go$', '^git$', '^npx$', '^postcss$', '^tailwindcss$', '^asciidoctor$']

[[menus.main]]
  name = "Features"
  url = "/#features"
  weight = 10

[[menus.main]]
  name = "Screenshots"
  url = "/#screenshots"
  weight = 20

[[menus.main]]
  name = "Docs"
  url = "/docs/"
  weight = 30

[[menus.main]]
  name = "Blog"
  url = "/blog/"
  weight = 40

[[menus.main]]
  name = "Download"
  url = "/download/"
  weight = 45

[[menus.main]]
  name = "GitHub"
  url = "https://github.com/tonk/warmdesk"
  weight = 50
  [menus.main.params]
    external = true
    icon = "github"
